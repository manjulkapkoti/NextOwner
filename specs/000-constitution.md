# Spec 000 — Project Constitution

> Binding decisions for **all** feature specs in this project. Every `spec.md` / `plan.md` under `specs/` must comply with this document. Amending an article requires updating this file (with date + reason) and checking existing specs for impact.
>
> Reference documents: [`requirements.md`](../docs/requirements.md) (**NextOwner's** requirements — MVP scope F1–F12, FR-1…FR-23, NFRs), [`design_implementation.md`](../docs/design_implementation.md) (architecture & milestones), [`testing_guide.md`](../docs/testing_guide.md) (test framework & per-milestone checklists), [`agentic_scope.md`](../docs/agentic_scope.md) (future AI roadmap), [`research/`](../docs/research/) (competitor teardowns + the rejected Supabase option — **reference only, binding on nobody**).

---

## Article 1 — Tech Stack (DECIDED 2026-07-13)

| Layer | Decision |
|---|---|
| Frontend | **React + Vite + TypeScript**, MUI (+ Emotion), MobX, react-router |
| Backend | **Python FastAPI** — the API is the only door to data |
| ORM / models | **SQLModel** (SQLAlchemy + Pydantic) |
| Database | **SQLite** locally now → **PostgreSQL** later (connection-string swap; no code rewrite) |
| Auth | JWT issued by our own FastAPI endpoints; bcrypt password hashing |
| Realtime (chat) | FastAPI **WebSockets** (polling acceptable as interim fallback) |
| File storage | Local `uploads/` folder, served only through permission-checked endpoints |
| Backend tests | **pytest** + FastAPI TestClient, in-memory SQLite per test |
| Frontend tests | Vitest + React Testing Library; Playwright E2E after Phase D |
| Third-party vendors (Stripe, Persona, Escrow.com, ChartMogul) | **Mocked locally** with production-shaped state machines; real integrations are post-MVP |
| Environment | **100% local** — no cloud account, no Docker required (Docker becomes optional only for the Postgres swap) |

**Rationale:** Python aligns with the planned agentic layer (`agentic_scope.md`); FastAPI's Pydantic validation and auto-generated `/docs` fit spec-driven development; SQL skills are the most transferable.

**Agent-readiness (recorded 2026-07-13)** — this stack was confirmed as the recommended base for owner-controlled agent development, for structural reasons that must be preserved as the code grows:

- *Same language:* agents are ordinary Python modules in the same service as the business logic — no cross-service glue to give an agent a tool. (Competitive note: no company in the researched category runs Python; the only shipped AI feature, Baton's Elena, was built against a TS stack.)
- *Permissions constrain agents structurally:* an agent runs **as** a scoped user through the same `permissions.py` gates (Article 2 applies to agents exactly as to humans) — it physically cannot exceed the rights of the identity it acts for. Never grant agents a rules-bypassing super-identity.
- *State machines + audit rows* (Article 2 #3, #5) mean agent actions go through the same legal-move validation and leave the same trace as human actions.
- *Pydantic* validates agent tool arguments and structured LLM outputs with the same schema system used for API requests; *WebSockets* (chat) are the reuse path for streaming agent progress; *SQL (Postgres)* hosts agent run logs, comps, and the **canonical** data, while **vectors live in a local vector store (Qdrant)** behind a `VectorStore` interface (pgvector kept as an option), populated by a **local embedding model** — Qdrant is a *rebuildable* index, so Postgres stays the source of truth.
- *Planned additions when agents arrive (additive, not corrective):* a job runner + `agent_runs`/`agent_steps` tables for long-running execution and tracing; golden-set evals as pytest cases; the Postgres swap likely moves earlier once concurrent agent writes appear.
- *Principle:* agent loops are built on a **thin, swappable model interface** — provider-agnostic, so a **local / open-source model** is a drop-in for a frontier one — with tools exposed via plain functions/**MCP**. **Prefer an owned thin loop; a framework may be adopted if the situation warrants (decided at build time).** Non-negotiable regardless: agents run **as scoped users through the gates**, and the security boundary lives in the core — not in the framework or the model.

**Considered and rejected:**
- *Firebase (Acquire's real stack, via Emulator Suite)* — maximum case-study fidelity and free realtime, but TypeScript-only backend and proprietary rules language. Documented in [`docs/research/acquire_design.md`](../docs/research/acquire_design.md) §2–§4 *(was `design_implementation.md` Part 2 until 2026-07-16)*.
- *Supabase (BaaS, no custom backend)* — fastest path, but less is learned and backend logic wouldn't be Python. Kept as reference in [`docs/research/supabase_alternative.md`](../docs/research/supabase_alternative.md) *(was `design_implementation.md` Part 6 until 2026-07-16)*.

## Article 2 — Architecture Principles

1. **The API is the only door.** The browser never talks to the database; every privilege check lives in a FastAPI dependency (`permissions.py`) — one function per trust boundary.
2. **Public/private split.** Anonymous listing data and NDA-gated data live in separate tables served by separate endpoints; public response models must make identity-field leaks impossible by schema.
3. **Status state machines are the business.** `listing.status`, `offer.status`, `access_request.status` encode the workflow; transitions happen only inside endpoints that validate the move. Clients never set status fields directly.
4. **Never trust the client** for `owner_id`, `sender_id`, `status`, prices in privileged flows — the server derives them from the JWT and the database.
5. **Audit what matters:** offers and access decisions get timestamped event rows.

## Article 3 — Development Process

1. **Spec-driven:** each milestone (Part 4 of `design_implementation.md`) gets `specs/NNN-name/spec.md` (user stories + GIVEN/WHEN/THEN acceptance criteria + FR references) and `plan.md` (schema deltas, endpoints, components, **and the Build order** — the implementation slices, one trust boundary each) *before* implementation. Spec just-in-time — one or two milestones ahead, no further.
2. **Tests are the acceptance criteria:** every GIVEN/WHEN/THEN becomes exactly one test (`testing_guide.md`); write them failing before implementing.
3. **Definition of done:** a milestone is done when its tests pass **and** the full `npm test` suite is green. Work happens on a per-milestone feature branch off `main`; commit freely on the branch (including the failing-tests-first commit). **Before the PR is opened**, the work is reviewed and tested **on the branch** (a pre-PR gate): the orchestrator reviews inline every milestone (architecture + the security must-cover matrix), plus one independent `appsec-engineer` pass on the security-critical milestones (M1/M2/M5/M7/M8/M10) — opening a PR signals the work is vetted and ready for human approval. `main` is updated **only by merging a PR that is green** (tests + the security must-cover matrix) — never by a direct commit, so `main` is always green.
4. **Milestone order is binding** (M0→M12 as sequenced in Part 4 — M12 appended 2026-07-16); Phase E items (M8–M11) may be reordered among themselves.

## Article 4 — Conventions

- **Product name: NextOwner** (decided 2026-07-13). Use it in all user-facing strings, app titles (`FastAPI(title="NextOwner API")`), and branding; local SQLite file is `nextowner.db`. The repo folder is `NextOwner`. Before public launch: verify domain availability and run a USPTO trademark search (class 35/36).
- Folder layout as defined in `design_implementation.md` §3.3 (`app/`, `backend/app/{routers,services}`, `backend/tests/`, `seed/`, `specs/`).
- REST style: plural nouns (`/listings`), sub-resources for ownership (`/listings/{id}/private`), POST verbs only for state transitions (`/listings/{id}/submit`, `/offers/{id}/accept`).
- **Single-origin layout:** all backend routes mounted under the `/api` prefix (WebSockets under `/ws`); locally the Vite dev proxy forwards both to FastAPI (no CORS); production uses one domain with path routing (reverse proxy → SPA build + FastAPI). Doc prose may omit the `/api` prefix; code never does.
- **NDA model:** one platform-wide NDA signed once per user (`users.nda_signed_at`), plus per-listing access requests approved by the seller (`requested → approved|denied`). Adopted from Baton research (`docs/research/baton_design.md`).
- Error codes: 401 unauthenticated, 403 forbidden, 404 not found, 409 invalid state transition, 422 validation (Pydantic default).
- Feature flags in a plain `flags.py` / `flags.ts`; analytics through a local `track(event, props)` wrapper (console only for now).
- Spec numbering: `000` constitution, then `001+` in build order matching milestones.

---

*Amendment log:*
- 2026-07-13 — v1. Initial constitution; backend stack changed from the original Firebase-mirror plan to Python FastAPI (see Article 1).
- 2026-07-13 — Product named **NextOwner** (Article 4).
- 2026-07-13 — Adopted from Baton research: one platform-wide NDA + per-listing access approval (affects Milestone 5 / FR-13), and single-origin `/api` path layout with Vite dev proxy (Article 4).
- 2026-07-13 — Agent-readiness note added to Article 1: stack confirmed as the recommended base for owner-controlled agent development; agents bound by the same permission gates as humans; no agent frameworks — loops built on the provider SDK.
- 2026-07-14 — Adopted a per-milestone feature-branch → PR → squash-merge workflow; the green gate is the PR merge, not individual branch commits (Article 3 §3).
- 2026-07-15 — Moved the `tech-lead` + `appsec-engineer` review-and-test to **before** the PR is opened (a pre-PR gate on the branch); opening a PR now means the work is agent-vetted and ready for a human. `/dod` is the green gate only (no longer opens the PR). Added a conflict-recovery procedure for concurrent PRs (`docs/git_strategy.md` § Two open PRs). (Article 3 §3.)
- 2026-07-15 — Made the pre-PR review **inline-first** to conserve context/usage: the orchestrator reviews every milestone inline (architecture + §8 matrix); a single independent `appsec-engineer` agent (diff-scoped, background, Sonnet — Opus for M5) is added only on the security-critical milestones (M1/M2/M5/M7/M8/M10). The `/dod` forbidden-path tests remain the always-on security floor. (Article 3 §3.)
- 2026-07-16 — Agentic-stack choices recorded (Article 1 agent-readiness), superseding the 2026-07-13 "provider SDK / no frameworks" note: the agent layer is **provider-agnostic behind a thin model interface** (a **local/open-source model** is a drop-in for frontier); vectors move to a **local Qdrant** store behind a `VectorStore` interface (**local embedding model**; pgvector kept as an option; Qdrant is a *rebuildable* index, Postgres stays the source of truth); and **agent frameworks become a build-time option** rather than banned. Non-negotiables kept: agents run as scoped users through the gates, and tools stay plain functions/MCP.
- 2026-07-17 — **M1 spec decisions resolved (D1/D2), owner-approved.** **D1 — refresh tokens deferred to an *owner*, not to limbo:** M1 ships **access-token-only**; refresh + the httpOnly-cookie session it needs move to the deploy-hardening bucket (`security.md` §9 — *Session hardening*), triggered by *real deployment / real users*. This completes FR-1's deliberate amendment (its OAuth half was marked post-MVP earlier today; this is the **refresh half**, met by the fold-in's *"amend, don't drift"* rather than by silent omission). The deferral is safe because the M1 spec **states its token-storage approach** (`localStorage` now → httpOnly cookie + refresh at the trigger) — the seam that keeps refresh a clean later-add, since `get_current_user` trusts the JWT not the auth method and `api.ts` is the single frontend choke point (the same structural property that makes Google OAuth a later-add). **D2 — M1's Build order accepted at 12 slices:** the `error_handling.md` §7 error-contract foundation is M1-mandated infrastructure with no FR of its own, so it stays in M1 rather than becoming a featureless milestone or forcing an M2–M12 renumber; **slice-by-slice building is the mitigation for milestone size**, so 12 small verifiable slices is acceptable, not a regression of the right-sizing work.
- 2026-07-17 — **Implement in ordered slices; M1 right-sized** (Article 3 §1, §3). **(1) `plan.md` gains a "Build order" section.** Every other `plan.md` section says *what will exist when the milestone is done*; none said *in what order to build it* — so `/run-milestone` step 5 read as one big-bang push to green. A **slice = one trust boundary** (Article 2 #1 — the decomposition falls out of the architecture, not out of the test list), turns a named cluster of red tests green, and ends in one commit. **The section deliberately has no checkboxes and no status:** `plan.md` fixes the *order* (a design decision); the **red test list is the status**, which is exactly why `/resume` can rebuild a dead session from git + tests with no status file. A ticked box would be a second source of truth that lies after the first crash — the failure mode the 2026-07-16/17 doc audits kept finding. **(2) M1 right-sized:** the fold-in's own escape hatch ("product-lead may split the account-lifecycle items if M1 balloons") is taken — but by **moving one fold-in, not by adding a milestone** (a new M1b would shift the spec-number column and force the security-critical list to change in 13 places, for no product gain). **Account lifecycle (password reset + email verification) moves M1 → M8**, which builds the email channel anyway; M1's fold-in had that dependency backwards. Measurement note: M1 looked like "9 fold-ins" but only **one** was heavy, one was a decision, and one (the OAuth exclusion) was zero work — *counting bullets overstated it; the fix was to move the single heavy item.* **(3) M8 is now security-critical** (list: M1/M2/M5/M7/**M8**/M10, updated in all 13 places): a password-reset token is account takeover if it leaks — single-use, short expiry, hashed at rest, uniform response (no user enumeration), never logged. M8 is **not** ⭐ — that stays M5. **Accepted trade-off:** M1–M7 ship with no self-serve password reset and no verified addresses; fine while 100% local with no real users, and this is a scoping call, so it reverses if real users arrive first.
- 2026-07-17 — **Pre-M1 audit — three doc contradictions fixed** (no code changed; the scaffold, tests and CI were verified sound). **(1) `requirements.md` contradicted itself:** its §3 note still opened *"This file is a research teardown of Acquire"* — the binding requirements doc declaring itself non-binding research, 72 lines under a header saying the opposite. The note travelled verbatim in the 2026-07-16 split below; rewritten to keep its still-true warning (the NFR cells speak Acquire's vocabulary — read the requirement, never the mechanism). **(2) FR-1 promised Google OAuth** with no deferral marker, while `design_implementation.md` Part 4 had already ruled it post-MVP — and *only* Part 4 said so (0 mentions in the fold-ins, the testing checklist, or `security.md` §7). Since `/new-spec` cites the binding doc, M1 could have inherited an OAuth acceptance criterion. **FR-1 is now email+password, OAuth marked (Post-MVP)** — deferred both for the learning goal (own the JWT mechanics) and because a hosted provider needs the cloud account Article 1 excludes — with a matching M1 scope fold-in. *Note the near-miss: the gap review applied exactly this drift-check to FR-1's **refresh-token** half and missed its OAuth half, in the same sentence.* **(3)** the Firebase "considered and rejected" pointer above still aimed at `design_implementation.md` Part 2, now a stub — repaired to `research/acquire_design.md` §2–§4 (the adjacent Supabase pointer was fixed in the split; this one was missed one line above it). Also corrected: F1's *"Firebase Auth makes it ~free"* rationale — Acquire's mechanism, not ours.
- 2026-07-16 — **Research split from requirements** (Article 4 — doc layout). `acquire_design.md` was a **teardown of a competitor doing double duty as the requirements source of truth**, and the two jobs had fused: its FRs had been adapted for NextOwner while its NFR table still spoke Acquire's stack — which is how a rejected architecture came to prescribe our scaling strategy (see the entry below). One file, one job, from now on: **`docs/requirements.md`** holds NextOwner's MVP scope (F1–F12) + FR-1…23 + NFRs and is what **specs cite**; **`docs/research/acquire_design.md`** holds the teardown, joining its siblings — **reference only, binding on nobody**. `design_implementation.md` got the same treatment, since it too was half someone else's product: **Part 2** (Acquire's architecture walkthrough) moved into `research/acquire_design.md` §4, *reuniting it with the diagram in §3 that it had always described from another file*, and **Part 6** (the rejected Supabase option) moved to `research/supabase_alternative.md`. Parts 1/3/4/5 stay — Part 1 is NextOwner's own domain knowledge and owns the business-workflow diagram — and the Part **numbering is unchanged** (2 and 6 are now pointer stubs) so the ~10 existing references to "Part 4" still resolve. The implementation guide drops 864 → 532 lines, **100% of it NextOwner's** (was 45%). **The rule this encodes:** a teardown's findings are worth keeping *as findings* — the failure mode is losing the attribution, not recording the fact.
- 2026-07-16 — **Horizontal scale recorded as deferred-but-designed** (Article 2). The **`requirements.md` §3** (then `acquire_design.md` §7) **Scalability** NFR demanded 100k listings / 500k users *"without re-architecture"* while prescribing *"serverless auto-scaling (Functions/Firestore) preferred"* — the stack Article 1 rejected. That promise held for Acquire **because** Firebase makes per-instance state impossible; inherited into long-running FastAPI processes it kept the promise and lost the mechanism. The NFR is rewritten (plus a status note: its cells still speak Acquire's stack — read the requirement, not the mechanism), and three **per-instance-state blockers** are named with fixes in `design_implementation.md` § *Horizontal scale*: auth rate limiting (**M1**), local-disk uploads (**M2**), and the in-memory WebSocket registry (**M6** — fails *silently* behind a load balancer). **JWT statelessness is the load-balancer prerequisite the architecture already has.** No build work now — the MVP implementations stay; each must simply sit **behind a small interface** (the same swappable-port idiom Article 1 uses for the agentic layer), recorded as scope fold-ins so M1/M2/M6 build the seam instead of retrofitting it. Also added to `security.md` §9: **mTLS belongs on internal hops** (backend→Postgres, backend→vendor), explicitly **not** at the browser edge.
- 2026-07-16 — End-to-end gap review (Article 3 §4): appended **M12 — Deal completion** to the milestone sequence — the `under_offer → sold` / `→ live` (fell-through) half of the state machine, with the final sale price **server-derived from the accepted offer**; the E2E golden path extends to "sold". Expanded **M8** into the general **notifications engine** (FR-22 + the FR-16 email fallback): M3/M5/M6/M7 emit notification events, M8 delivers them alongside the saved-search alerts. Per-milestone **scope fold-ins** recorded in `docs/milestones.md` § Scope fold-ins (account lifecycle + minimal profiles + `tos_accepted_at` in M1; listing lifecycle + `Decimal` money in M2; `listing_event` audit in M3; keyword search in M4; revocation endpoint + `nda_version` in M5; unread counts + WS error contract in M6; counter/sibling-offer mechanics in M7; upload quotas in M10; lead capture in M11) — `/new-spec` reads them at spec time. the MVP features (now `requirements.md` §1) renumbered **F1–F12** (were M1–M12) to end the collision with milestone numbers. Payments (existing ⚠) and trust-&-safety/admin-ops (new ⚠) remain unsequenced — `product-lead` to slot.
