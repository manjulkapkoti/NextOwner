# Python / FastAPI backend
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Local database + uploaded data-room files (created at Milestone 0+)
nextowner.db
*.db-journal
backend/uploads/
uploads/

# Node / Vite frontend
node_modules/
dist/
build/
*.local

# Test / E2E artifacts
coverage/
playwright-report/
test-results/

# Env & secrets
.env
.env.*
!.env.example

# Claude Code — personal, machine-local settings
CLAUDE.local.md
.claude/settings.local.json
# session flight recorder — local, per-machine
.claude/session-state.md

# Personal / portable docs (kept local, not part of this repo)
remember_todo.md
PRODUCT_PLAYBOOK.md
PRODUCT_PLAYBOOK_DESKTOP.md
docs/temp_readme.md

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Playwright screen captures — artefacts for looking at, not for review
app/e2e/__screens__/

# Captured command output — scratch artifacts from test/debug runs. One of these
# (backend/count_out.txt) reached `main` via an uninspected `git add -A`; the
# habit fix is reading `git status` before staging, this is only the backstop.
*_out.txt
scratch_*
*.bak
