[pytest]
# Put backend/ on sys.path so tests import the app as ``app.*`` (matches
# how the app runs). ``pythonpath`` is relative to this file's dir (rootdir).
pythonpath = .
testpaths = tests
addopts = -q
